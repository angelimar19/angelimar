/*let array=['angelimar','eleida','morillo','centeno']
console.log(array[0])*/

/*let datos=['angelimar', 17, 162, pizza]
console.log(datos)*/

let array=[true, 6, 'hola']
array.push(24)          // el push agrega el 24 como el ultimo//
console.log(array)

let array=[true, 6, 'hola']
array.pop()  //el pop elimina el ultimo valor//
console.log(array)

let array=[true, 6, 'hola']
array.unshift(24) //igual que el push solo que lo agrega al principio//
console.log(array)

let array=[true, 6, 'hola']
array.shift() // igual que el pop pero borra el primer elemento o valor//
console.log(array)